spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:local
    username: prism-local
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2
      settings:
        web-allow-others: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

jwt:
  secret: local-secret
